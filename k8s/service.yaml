apiVersion: v1
kind: Service
metadata:
  name: utah-public-lands
  namespace: schmidtdse
  labels:
    app: utah-public-lands
spec:
  type: ClusterIP
  selector:
    app: utah-public-lands
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
